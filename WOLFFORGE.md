# WolfForge experiment

The table contains BayesWolf, CharmWolf, ChaosWolf, and two base controls. Every
agent uses the same OpenRouter model and decoding settings. `harness` selects only
the strategy prompt. A profile is a policy, not a fixed ONUW (One Night Ultimate Werewolf) role: every agent
receives random roles over the run.